# Bug修复指南

## 已修复的问题

### 问题1：定时交易初始化失败 ❌ → ✅

**原因**: API路由未在主应用中注册

**修复**:
1. 在 `backend/src/api/app.py` 中添加导入:
   ```python
   from src.api.routes import ..., smart_strategy, scheduled_trading
   ```

2. 注册路由:
   ```python
   app.include_router(smart_strategy.router, prefix="/api", tags=["Smart Strategy"])
   app.include_router(scheduled_trading.router, prefix="/api", tags=["Scheduled Trading"])
   ```

### 问题2：智能构建器下拉框为空 ❌ → ✅

**原因**: 前端API调用路径缺少 `/api` 前缀

**修复**:
1. 修改 `frontend/src/api/smartStrategy.ts`:
   - 所有路径从 `/smart-strategy/*` 改为 `/api/smart-strategy/*`

2. 修改 `frontend/src/api/scheduledTrading.ts`:
   - 所有路径从 `/scheduled-trading/*` 改为 `/api/scheduled-trading/*`

## 验证修复

### 1. 重启后端服务器

```bash
cd BTCOptionsTrading/backend
python run_api.py
```

### 2. 检查API文档

访问 http://localhost:8000/docs

应该能看到:
- Smart Strategy 标签
- Scheduled Trading 标签

### 3. 测试智能构建器

1. 打开前端 http://localhost:3000
2. 进入策略管理
3. 点击"🧠 智能构建"
4. 下拉框应该显示选项:
   - 到期日: 明天、一周、一个月等
   - 行权价: ATM、ITM+1、OTM+1等

### 4. 测试定时交易

1. 点击"⏰ 定时交易"
2. 点击"配置API凭证"
3. 输入Deribit测试网API密钥
4. 点击"初始化"
5. 应该显示"初始化成功"

## 常见问题

### Q: 仍然看不到下拉选项？

A: 检查浏览器控制台是否有错误:
```
F12 → Console
```

查看网络请求:
```
F12 → Network → 筛选 XHR
```

### Q: 定时交易初始化仍然失败？

A: 检查:
1. API密钥是否正确
2. 是否选择了"使用测试网络"
3. 网络连接是否正常
4. 后端日志是否有错误

### Q: 如何查看后端日志？

A: 
```bash
tail -f BTCOptionsTrading/backend/logs/app.log
```

## 测试清单

- [ ] 后端服务器正常启动
- [ ] API文档显示新路由
- [ ] 智能构建器下拉框有选项
- [ ] 可以预览合约信息
- [ ] 可以构建策略
- [ ] 定时交易可以初始化
- [ ] 可以添加定时策略
- [ ] 可以查看账户信息

## 如果问题仍然存在

1. **清除浏览器缓存**
   ```
   Ctrl+Shift+Delete → 清除缓存
   ```

2. **重新安装依赖**
   ```bash
   # 后端
   cd backend
   pip install -r requirements.txt
   
   # 前端
   cd frontend
   npm install
   ```

3. **检查Python版本**
   ```bash
   python --version  # 应该是 3.7+
   ```

4. **检查Node版本**
   ```bash
   node --version  # 应该是 14+
   ```

## 联系支持

如果以上步骤都无法解决问题，请提供:
1. 浏览器控制台错误截图
2. 后端日志最后50行
3. 操作系统和版本
4. Python和Node版本
